# تحويل تطبيق مصمم الديكور الذكي إلى تطبيق EXE

## 📋 المتطلبات

- **Node.js** (الإصدار 14 أو أحدث)
- **npm** أو **yarn**
- **Windows 10/11** (لبناء ملفات EXE)
- **Visual C++ Build Tools** (اختياري، لتسريع البناء)

## 🚀 خطوات التثبيت والبناء

### 1️⃣ تثبيت المتطلبات

```bash
npm install
```

أو إذا كنت تستخدم yarn:

```bash
yarn install
```

### 2️⃣ تشغيل التطبيق في وضع التطوير

```bash
npm start
```

### 3️⃣ بناء تطبيق EXE للويندوز

#### خيار 1: برنامج تثبيت NSIS (موصى به)

```bash
npm run build-win
```

سيتم إنشاء:
- ملف `setup.exe` - برنامج تثبيت الويندوز
- ملف `portable.exe` - نسخة محمولة بدون تثبيت

#### خيار 2: ملف محمول فقط

```bash
npm run build
```

سيتم إنشاء ملف EXE محمول في مجلد `dist/`.

## 📁 موقع الملفات المُنتجة

بعد البناء الناجح، ستجد الملفات في:

```
dist/
├── setup.exe                          (برنامج التثبيت)
├── مصمم الديكور الذكي-portable.exe   (النسخة المحمولة)
└── unpacked/                          (الملفات المفكوكة)
```

## ⚙️ تخصيص البناء

### تغيير الأيقونة

1. استبدل ملف `assets/icon.png` بأيقونتك الخاصة
2. الحجم الموصى به: **512x512 بكسل**
3. الصيغة: **PNG شفافة**

### تعديل معلومات التطبيق

عدّل `package.json`:

```json
{
  "name": "اسم-التطبيق",
  "version": "1.0.0",
  "description": "وصف التطبيق",
  "author": "اسمك"
}
```

## 🌍 الميزات المدعومة

✅ **دعم اللغات الثلاث**
- 🇸🇦 العربية (RTL - من اليمين لليسار)
- 🇬🇧 الإنجليزية (LTR - من اليسار لليمين)
- 🇫🇷 الفرنسية (LTR - من اليسار لليمين)

✅ **وظائف سطح المكتب**
- حفظ واستعادة البيانات محلياً
- قائمة تطبيق احترافية
- اختصارات سطح المكتب وقائمة ابدأ
- تصميم ثلاثي الأبعاد (3D Canvas)

✅ **التوافقية والأمان**
- Windows 64-bit
- يعمل بدون الاتصال بالإنترنت
- عزل السياق (Context Isolation)
- بيئة وضع آمن (Sandbox)

## 🔧 استكشاف الأخطاء والمشاكل

### المشكلة: فشل التثبيت

**الحل:**
```bash
npm cache clean --force
rm -rf node_modules
npm install
```

### المشكلة: خطأ عند البناء

**تحقق من:**
- تثبيت Node.js صحيح: `node --version`
- وجود ملف `package.json`
- الاتصال بالإنترنت

### تمكين وضع التطوير

في `main.js`:
```javascript
if (isDev) {
  mainWindow.webContents.openDevTools();
}
```

## 🖥️ البناء للأنظمة الأخرى

### بناء لـ macOS:
```bash
npm run build-mac
```

### بناء لـ Linux:
```bash
npm run build-linux
```

## 📦 توزيع التطبيق

### للتوزيع الرسمي:

1. **توقيع الشهادة** (اختياري لكن موصى به)
   - احصل على شهادة توقيع من Microsoft
   - استخدمها لتوقيع التطبيق

2. **الاختبار**
   ```bash
   dist/setup.exe
   ```

3. **الرفع**
   - اختبر على أجهزة مختلفة
   - تأكد من عمل جميع الميزات

## 📚 مراجع إضافية

- [Electron Documentation](https://www.electronjs.org/docs)
- [Electron Builder](https://www.electron.build/)
- [NSIS Installer](https://nsis.sourceforge.io/)

## 💡 نصائح مهمة

⚠️ **قبل البناء:**
- اختبر التطبيق بالكامل في وضع التطوير
- تأكد من أن جميع الملفات موجودة (index.html, manifest.json, sw.js, i18n.js)
- تحقق من أيقونة التطبيق

⚠️ **بعد البناء:**
- اختبر ملف EXE على جهاز نظيف (بدون Node.js)
- جرّب النسخة المحمولة والمثبت
- اختبر جميع اللغات والوظائف

## 🎯 الخطوات السريعة

```bash
# 1. تثبيت
npm install

# 2. اختبار
npm start

# 3. بناء EXE
npm run build-win

# 4. ستجد الملف في:
# dist/setup.exe
```

## 🚨 ملاحظة أمنية

عند توزيع التطبيق:
- استخدم بروتوكول HTTPS إن أمكن
- وقّع التطبيق بشهادة رسمية
- اختبره بأدوات فحص الفيروسات
- اعرض سياسة الخصوصية للمستخدمين

---

**تم إعداد التطبيق بواسطة:** مصمم الديكور الذكي  
**الإصدار:** 1.0.0  
**آخر تحديث:** 2026-07-31
